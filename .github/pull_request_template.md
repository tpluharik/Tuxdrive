## Summary

Describe the user problem and the proposed change.

## Behaviour and safety

- Previous behaviour:
- New behaviour:
- Synchronization/deletion/conflict implications:
- Configuration or migration implications:

## Validation

- [ ] `PYTHONPATH=src python3 -m unittest discover -s tests -v` passes.
- [ ] `PYTHONPATH=src python3 -m compileall -q src` passes.
- [ ] Tests cover new or changed behaviour.
- [ ] User documentation is updated when necessary.
- [ ] Architecture, configuration, operations, security, testing, roadmap, platform and release docs are updated when their contracts change.
- [ ] Current version, package names, test counts and durable release links agree across documentation and manifests.
- [ ] No credentials, OAuth tokens, rclone configuration, private logs, or personal paths are included.
- [ ] The change preserves Ubuntu 26.04 support.

## Screenshots or logs

Attach sanitized evidence for user-interface or runtime changes when useful.
